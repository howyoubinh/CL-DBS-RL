name: cl-dbs-rl
channels:
  - conda-forge
dependencies:
  - python=3.9
  - numpy=1.26.2
  - scipy=1.11.4
  - pandas
  - matplotlib=3.8.2
  - numba=0.59.0
  - tqdm=4.66.*
  - pip
  - pip:
      # Core RL / spiking stack
      - torch==2.5.1
      - gymnasium==0.29.1
      - rockpool==2.9.1
      - wandb==0.21.1
      # Neuromorphic hardware deployment (optional: only for the Xylo
      # export/benchmark/validate scripts)
      - samna==0.45.3
